const styles = ({ typography, palette }) => ({
  drawerHeader: {
    "display": "flex",
    "align-items": "center",
    "justify-content": "space-between",
    "font-weight": "bold",
    "margin-bottom": "15px",
    "margin-left": "5px",
  },
  searchBox: {
    "margin-bottom": "25px",
  }
});

export default styles;
